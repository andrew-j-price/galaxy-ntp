---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: galaxy-ntp
      image: ubuntu
      image_version: latest
      ansible_groups:
        - group1
verifier:
  name: testinfra
